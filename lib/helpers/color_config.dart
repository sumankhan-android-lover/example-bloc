import 'package:flutter/material.dart';

class AppThemeColor{
  // Define your color scheme for light mode
  static const Color primaryColor = Color(0xff53B175);

  //define common color
  static const Color transparent = Colors.transparent;
  static const Color white = Colors.white;

  static const Color pageBackgroundColor = Color(0xFFFEFEFE);

  static const Color lightBlack = Color(0xff1c1b1f);
  static const Color black = Colors.black;
  static const Color extraLightBlack = Color(0x1f000000);
  static const Color deepBlue = Color(0xFF181725);
}